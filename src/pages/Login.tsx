import React, { FC } from "react";

import { LoginForm } from "../components/LoginForm";

const Login: FC = () => {

    return (
        <div>
            <h2>Login Page</h2>
            <LoginForm />
        </div>
    );
};

export default Login;
